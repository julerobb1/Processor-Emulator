{
  "configurations": [
    {
      "type": "coreclr",
      "request": "launch",
      "name": "Launch Processor Emulator",
      "program": "${workspaceFolder}/bin/Release/net6.0-windows/${input:executableName}.exe",
      "preLaunchTask": "build"
    }
  ],
  "inputs": [
    {
      "type": "promptString",
      "id": "executableName",
      "description": "Enter the name of the executable to debug (e.g., ProcessorEmulator)"
    }
  ]
}