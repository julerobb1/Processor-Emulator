 #if false
// You must cut down the mightiest tree in the forest with a herring!
// Duplicate CLI entry point disabled to resolve multiple Main definitions
#if false
// Program.cs disabled to remove duplicate Main entry point
#endif
